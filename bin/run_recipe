"use strict";

const fs = require('fs');

const runner = require('../lib/RecipeFolder').RecipeFolder;
const path   = require('path');
const filename = process.argv[2];

(new runner(filename));//.ready.then((recipes) => console.log(recipes));